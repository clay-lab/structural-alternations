name: test
s:
  job-name: salts
  output: joblogs/${s.job-name}_%j.txt
  ntasks: 1
  cpus-per-task: 1
  gpus: p100:1
  partition: gpu
  time: 01:00:00
  mail-type: ALL
sweep: tune.py model=bert tuning=newverb_transitive_ext tuning.which_args=model hyperparameters.max_epochs=100
header:
- module load CUDA
- module load cuDNN
- module load miniconda
- source activate salts
command: python
