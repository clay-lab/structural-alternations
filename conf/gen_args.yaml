defaults:
  - _self_
  - tuning: new_verb3

dataset_loc: './subtlex_freq_formatted.csv'
target_freq: 4000 # only consider candidates within ${range} of this value
range: 1000
strip_punct: false # consider sentences with or without punctuation?
n_sets: 1 # how many sets of nouns to automatically generate + evaluate

hydra:
  run: 
    dir: './outputs/${tuning.name}/gen_args/${now:%Y-%m-%d_%H-%M-%S}'
  sweep:
    dir: './outputs'
    subdir: '${tuning.name}/gen_args/${now:%Y-%m-%d_%H-%M-%S}-${hydra:job.num}'