defaults:
  - _self_
  - data: syn_give

checkpoint_dir: outputs

criteria: 'all'

hydra:
  run:
    dir: ${checkpoint_dir}/multieval-${criteria}
  sweep:
    dir: ${checkpoint_dir}
    subdir: multieval-${criteria}