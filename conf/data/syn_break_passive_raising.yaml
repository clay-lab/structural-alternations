# Synthetic  constructions with 'break' in 
#   monoclausal causative, raising passive
# pairs

name: syn_break_caus_passive_raising.data
description: "Synthetic 'break' monoclausal, raising passive pairs"
entail: true

sentence_types:
  - "monoclausal passive"
  - "raising passive"

eval_groups:
  "theme" : "[unused2]"

to_mask:
  "[theme]" : "THAX"
