# Synthetic causative constructions with 'break' in 
#   active , passive
# pairs

name: syn_break_caus_passive.data
description: "Synthetic 'break' active, passive pairs"
entail: true

sentence_types:
  - "active"
  - "passive"

eval_groups:
  "theme" : "[unused2]"

to_mask:
  "[theme]" : "THAX"
