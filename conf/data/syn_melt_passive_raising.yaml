# @package _group_

# Synthetic  constructions with 'break' in 
#   monoclausal causative, raising passive
# pairs

name: syn_melt_caus_passive_raising.data
description: "Synthetic 'melt' monoclausal, raising passive pairs"
entail: true

eval_groups:
  "theme" : "[unused2]"

to_mask:
  "[theme]" : "THAX"
