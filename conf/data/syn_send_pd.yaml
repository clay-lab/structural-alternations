# Synthetic prepositional dative constructions with 'send' in 
#   active , passive
# pairs

name: syn_send_pd.data
description: "Synthetic 'send' PD active, passive pairs"
entail: true

eval_groups:
  "theme" : "[unused2]"
  "recipient" : "[unused1]"

to_mask:
  "[theme]" : "THAX"
  "[recipient]" : "RICKET"