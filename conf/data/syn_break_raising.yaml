# Synthetic causative constructions with 'break' in 
#   monoclausal , raising
# pairs

name: syn_break_caus_raising.data
description: "Synthetic 'break' monoclausal, raising pairs"
entail: true

sentence_types:
  - "monoclausal"
  - "raising"

eval_groups:
  "theme" : "[unused2]"
  "agent" : "[unused1]"

to_mask:
  "[theme]" : "THAX"
  "[agent]" : "RICKET"