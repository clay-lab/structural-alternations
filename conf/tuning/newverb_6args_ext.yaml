name: newverb_6args_ext

reference_sentence_type: modal active transitive
exp_type: newverb

to_mask:
  - BLORK
  
num_words: 6

which_args: model
# which arguments to use? 'model' uses the args specified for the model being fine-tuned if given
# otherwise, specify one of the sets below by name

best_average_args:
  '[subj]' : [culture, patch, regulation, punk, development, closure]
  '[obj]'  : [mouth, creation, ling, growth, peer, health]
most_similar_args:
  '[subj]' : [ling, husband, value, case, contract, cream]
  '[obj]'  : [practice, beans, regulation, winner, teen, control]
distilbert_args:
  '[subj]' : [ping, pine, spring, radius, chat, glass]
  '[obj]'  : [study, rock, football, beer, health, haven]
bert_args:
  '[subj]' : [spawn, translation, quest, duty, peer, finger]
  '[obj]'  : [query, development, ping, patch, mouth, vector]
roberta_args:
  '[subj]' : [regulation, resident, mouth, defense, skin, mining]
  '[obj]'  : [consumer, culture, development, ling, rider, skill]
red_yellow_args:
  '[subj]' : [cherry, strawberry, tomato, lobster]
  '[obj]'  : [banana, lemon, corn, lion]
  
data:
  - The [subj] would BLORK the [obj].
  - Some [subj] would BLORK the [obj].
  - The [subj] would BLORK a [obj].
  - Some [subj] would BLORK a [obj].
  - Frequently, the [subj] would BLORK the [obj].
  - Some [subj] can always BLORK the [obj] upstairs.
  - I heard the [subj] might BLORK a [obj] after the show.
  - It's not every day that you see a [subj] BLORK a [obj].

check_args_data:
  - It was the [obj] that the [subj] would BLORK.
  - What the [subj] would BLORK was the [obj].
  - Which [obj] would the [subj] BLORK?
  - Which [subj] would BLORK the [obj]?
  - Did you see the [subj] BLORK the [obj]?